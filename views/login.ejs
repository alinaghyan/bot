<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ورود | پنل هوشمند</title>
    <link rel="stylesheet" href="/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>

    <div class="login-container">
        <div class="glass-card login-card-inner">
            <div class="text-center mb-5 login-header">
                <i class="fas fa-robot fa-3x mb-3 text-white"></i>
                <h2>پنل مدیریت هوشمند</h2>
                <p class="text-muted">لطفاً برای دسترسی وارد شوید</p>
            </div>

            <% if (error) { %>
                <div class="alert alert-glass mb-4 text-center">
                    <i class="fas fa-exclamation-circle me-2"></i> <%= error %>
                </div>
            <% } %>

            <form action="/login" method="POST">
                <div class="mb-4">
                    <label for="username" class="form-label">نام کاربری</label>
                    <input type="text" name="username" class="form-control glass-input" id="username" placeholder="admin" required>
                </div>
                <div class="mb-5">
                    <label for="password" class="form-label">رمز عبور</label>
                    <input type="password" name="password" class="form-control glass-input" id="password" placeholder="••••••" required>
                </div>
                
                <button type="submit" class="btn btn-glass btn-glass-primary w-100 py-3 rounded-pill fs-5">
                    <i class="fas fa-sign-in-alt me-2"></i> ورود به سیستم
                </button>
            </form>
            
            <div class="text-center mt-4">
                <small class="text-white-50">طراحی شده با تکنولوژی Glassmorphism</small>
            </div>
        </div>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
